name: depoy next js app

on:
 push:
  branches: [ main ]

jobs:
 build:
  runs-on: ubuntu-latest 

  concurrency:
   group: deploy
   cancel-in-progress: true

   steps:
    - name: checkout repository 
      uses: actions/checkout@v5.0.0
    - name: Install Dependencies
      run: npm install
    - name: Build Next.js App
      run: echo"Depolying App......"


 
